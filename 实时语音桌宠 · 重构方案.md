# 实时语音桌宠 · 重构方案

> 本文汇总本轮讨论结论，作为下一阶段重构的总纲。
> 
> 阅读顺序：**① 重构之后的架构 → ② AI 链路的修正 → ③ 存在的问题与修复方案**。
> 
> 版本说明：在飞书原稿基础上，补充了「成本再校准（文本 vs PCM / 合并进程）」「WASM 前端 ASR 的真实价值与坑」「partial 投机预热详解」，并补全了原稿中未能导出的表格。
> 
> 

---

## 1. 重构之后的架构

### 1.1 一句话定位

- 桌宠是**低延迟实时流系统**，不是离线编辑器。所有架构决策以「出声延迟」为第一优先级。

- Electron 主进程保持**很薄**：只负责配置注入、文件存储、ASR。

- 音视频**数据流永远不跨进程**（红线，见 1\.5）。

- 通过一个**共享总线**解耦模块，控制面板、Live2D、AI、以及未来的 Agent 都只是总线的读写方。

### 1.2 模块与职责

### 1.3 关键决策：控制面板是「消费者/写入方」，不是「桥」

控制面板确实需要连接 Electron（配置持久化）和 Live2D（展示参数）——这个需求成立，不可能在模型头上挂一个大菜单。但连接的**形态**要选对：

- ❌ **形态 A（隐患）**：数据从面板身上「穿过去」——Live2D 找面板要配置，面板再去问 Electron。面板一关，Live2D 断粮；Agent 形态下没有面板，整套驱动不了。

- ✅ **形态 B（采用）**：配置/参数存在共享总线里。面板是写入方（改参数 → 写总线），Live2D 是读取方（订阅总线）。面板与 Live2D 谁都不直接引用对方。

```Plain Text
控制面板 ──写──▶ sharedStore(展示参数) ◀──订阅── Live2D service
   │
   └──写──▶ sharedStore(配置)        ◀──读──── Electron(持久化)

Agent(未来) ──写──▶ sharedStore(指令) ◀──订阅── AI orchestration
```

效果完全一致（面板改参数、Live2D 生效），但面板从「数据必经的中转站」降级为「总线上的一个 writer」。面板关掉，总线里的参数还在，Live2D 照常运行。

### 1.4 关键决策：orchestration 留在 renderer

实时（非 Agent）形态下，orchestration 留 renderer 是**延迟最优**的，不要为想象中的 Agent 未来提前搬家。理由见 1\.5 的控制流/数据流拆分。

同时，只需**预留一个总线入口**即可平滑走向 Agent：orchestration 的核心是一个「接收文本 → 产出语音 \+ 动作」的函数，现在由 renderer 的语音输入触发，未来 Agent 从主进程往总线发一条消息也能触发。不用搬家，只留入口。

orchestration 落位对比：

**结论**：Live2D service 留 renderer（要 canvas/DOM，没得选）；orchestration 留 renderer \+ 预留总线入口。「脸」在 renderer，「大脑」可被总线消息驱动，Agent 未来只接总线。

### 1.5 红线：控制流可以跨进程，数据流绝不跨进程

链路里流动两种东西，必须区别对待：

- **控制流**（发起 LLM 请求、逐句 dispatch、触发 TTS）：都是小 JSON 消息，IPC 一跳亚毫秒级，放哪都无感。

- **数据流**（麦克风 PCM、TTS 音频）：绝不能为「编排」新增音频跨进程跳。Web Audio / LiveKit 全部留在 renderer。

这条红线解释了为什么「orchestration 留 renderer」和「未来 Agent 无头驱动」并不矛盾：Agent 发的是控制流（小消息），音频数据流始终在 renderer。

> **补充校准（见 2.3）**：现有架构里 PCM 确实存在「前端 ↔ Electron」这**唯一一跳**（麦克风在 renderer、ASR 在 Electron）。这是既有的必要跳，不违反红线；红线针对的是「为编排/合并而新增音频跨进程」。这一跳是否消除，正是 WASM 前端 ASR / onnxruntime\-node 要讨论的（见 2.3）。
> 
> 

### 1\.6 关于 service 契约与 adapter 的边界（YAGNI）

- **保留**：`IAiService` / `ILive2dService` 这类 interface \+ token 边界。成本极低，本身就是「容器装配 service」。

- **不引入**：adapter 运行时多后端切换机制。当前 ASR 固定在 Electron、TTS 固定在 Python，各自单一实现，没有「运行时选后端」的需求，硬上就是 YAGNI。

- **例外的处理**：ASR 曾尝试 WASM 前端化但未跑通，已退回 Electron node 侧——这是对的。将来若要在「Electron sherpa」与「WASM 前端」之间切换，在单一 ASR service 内部加开关即可。

---

## 2\. AI 链路的修正

### 2\.1 根因判断：级联架构的延迟是累加的

当前是**级联架构（cascade）**：ASR → LLM → TTS 三段串行，延迟累加。豆包实时语音、GPT\-4o realtime 那种「秒回」体感来自**端到端语音大模型（speech\-to\-speech）**，把三段塌缩成一个模型。

本项目用 Qwen \+ GPT\-SoVITS，注定是级联。**累加延迟是架构固有的**，调优只能压缩每一段、并让段与段尽量**重叠（pipeline overlap）**，而不是接力。这是全部修正的核心思路。

### 2\.2 延迟拆解（现状）

文本输入 → 语音回复 ≈ 3s：

```Plain Text
用户文本
 ├─[a] LLM 首字 TTFT           ~300-800ms
 ├─[b] 攒够第一句 speakText    ~200-500ms（等 JSON Lines 一句成形）
 ├─[c] DataChannel 发 tts.speak ~5-20ms（本机可忽略）
 ├─[d] GPT-SoVITS 首块推理     ~300-800ms  ← 大头
 └─[e] Opus编码→SFU→抖动缓冲→播放 ~300-500ms ← 大头（抖动缓冲约 300）
```

语音输入 → 语音回复 ≈ 5\-9s：在上面基础上多了 ASR 跨进程往返，且若 sherpa 为「整句 VAD 结束后才识别」（非流式），是 5\-9s 的主因。

### 2\.3 成本再校准：合并进程不能消除级联；穿梭的是文本不是 PCM

> 本节修正本轮讨论中一个被高估的假设，是后续所有优化的定盘星。
> 
> 

**\(1\) 穿梭主体是文本，不是 PCM。** 实际链路：前端麦克风采 PCM → ASR\(Electron\) 识别 → 文本推前端 → 前端发文字 AI → 回复文本 → 前端发 TTS\(Python\) → 音频回前端。

- PCM 只在「前端 ↔ Electron」**一跳**。

- 跨进程流动的**主体是文本**（小 JSON，几十字节 \~ 几 KB），文本 IPC 亚毫秒级，本来就便宜。

- **结论**：把三段服务合并进一个进程「省传输」的收益有限，因为省的只是本就廉价的文本 IPC。（此前把「PCM 跨进程」当成主要成本，是误判。）

**\(2\) 合并进程 ≠ 消除级联。** 级联延迟的根源是**顺序依赖**（ASR final → LLM → TTS 的推理时间累加），不是进程边界。三段塞进同一进程，推理时间照样累加。

- 合并能砍掉的只有：连接握手 / 冷启 / 序列化等**固定开销**（几十毫秒级，是真收益但非大头）。

- 合并真正的价值：**让 overlap 几乎免费**——进程内 async generator / `asyncio.Queue` 直接传对象，而不是 localhost HTTP。这才是把 5\-9s 压下来的杠杆。

- 若要合并，现成框架：

    - **LiveKit Agents（首选）**：复用现有 LiveKit 传输，自带 VAD / turn detection / barge\-in，STT/LLM/TTS 可插拔（sherpa、gpt\-sovits、文字 AI 各写一个适配类）。

    - **Pipecat**：传输无关，同样内建 VAD/打断，想脱离 LiveKit 时更灵活。

    - 自撸 `asyncio` \+ WebSocket：控制最大，但 barge\-in/竞态/收尾等坑全得自己填。

- **铁律**：合并后子模块之间**别用 localhost HTTP 串**，否则 IPC 又回来了；要用同进程 async generator / Queue 直接传对象。

**\(3\) WASM 前端 ASR 的真实价值（校正）。** 把 ASR 用 WASM 搬到前端（sherpa\-onnx onnx\-web），价值**不是「省 PCM 传输」**（那跳本就不贵），而是：

- **干掉「前端 → Electron → 前端」整个往返**，含 IPC 序列化 \+ Electron 侧 ASR 调度排队。

- partial 在前端**本地零延迟产生** → 可立即做投机预热（见 2\.5）。

- 前端直接拿 PCM 做 VAD / 端点检测，barge\-in 判断更快。

两个坑（曾「没跑通」的常见元凶，重开先有预期）：

- **模型体积与加载**：zipformer 流式模型 WASM 化后首次可能几十 MB，要处理 IndexedDB 缓存与首屏加载策略。

- **线程 / SIMD**：onnxruntime\-web 高性能依赖 WASM SIMD \+ 多线程（`SharedArrayBuffer`），后者需页面开 **COOP/COEP 跨源隔离**（Electron 里配 `crossOriginIsolated`）。这常是「没跑通」的根因。

**等价平替（若 WASM 仍难）**：ASR 已在 Electron node 侧，可用 **onnxruntime\-node**（原生，不受 `SharedArrayBuffer` 限制、性能更好），再把 partial 结果通过 IPC **实时推前端**（文本极小，IPC 无压力）。投机预热的收益依赖「partial 能实时拿到」，**不依赖 WASM**。

### 2\.4 修正方向（借鉴公开范式，全部开源落地）

**A\. TTS 传输层**

- **首块优先**：合成引擎吐出第一个 chunk 立即推，chunk 带序列号，末包用负序列号标记。前端边收边播、可据序列号在 barge\-in 时丢弃晚到包。

- **固定 ogg\(Opus\) \+ streaming**：当前 `normalizeTransport` 把 wav \+ streaming 强制降级为非流式，意味着用 wav 时第一句必须整句合成完才有声。低延迟场景固定 `media_type: 'ogg'` \+ `streaming_mode: true`，让 GPT\-SoVITS 的切片直接边合成边推。**首包收益最大的一刀**。

- **一条持久连接**：句子只是连接上的 message，不每句新建。LiveKit Room 已复用，方向正确，保持。

**B\. LLM → TTS 增量喂入（借鉴 text\-in streaming）**

- 把喂给 TTS 的颗粒度从「整句」降到逗号/短语级（配合 GPT\-SoVITS 的 cut 策略），让第一段音频更早启动，压缩 \[b\]。

**C\. ASR 前端流式化**

- **边说边识别**：partial（中间结果）\+ final（确定结果）双结果，不等说完。

- **端点检测（endpointing）**：VAD 检测到停顿立即触发 final 进 LLM，而非静音超时。

- **投机预热**：partial 稳定时提前用 partial 文本预发 LLM 请求；final 一致则直接用，不一致则丢弃重发。把级联「偷」成重叠。**详见 2\.5**。

- **落地**：sherpa\-onnx 用**流式 zipformer transducer 的 online 接口 \+ endpoint**，不要用 offline 整段模式。ASR 放前端 WASM 还是 Electron node，见 2\.3\(3\)——投机预热两者皆可用。

**D\. 全双工 / barge\-in**

- **AEC 是命根子**：扬声器外放时麦克风会把 TTS 录回去，导致 ASR 自问自答。个人项目三档选择：

    1. 浏览器内置 AEC（`getUserMedia({ echoCancellation: true })`，已在用，先靠它 \+ 软 barge\-in）。

    2. 进阶：`webrtc-audio-processing` 编成 WASM，在 AudioWorklet 里跑 AEC3。

    3. 取巧（桌宠最优 ROI）：**耳机 \+ 状态门控**——戴耳机物理无回声；TTS 播放期间 ASR 只做 VAD 不识别，VAD 触发才打断。建议先走档 3 \+ 档 1 兜底。

- **barge\-in 策略**：VAD 一检测到用户开口，立即发 `tts.cancel` \+ 本地静音 `<audio>`。这是「能对话」到「能自然对话」的分水岭，当前缺失。

**E\. 抖动缓冲分档自适应**

- 现状固定 LOW 300 / HIGH 900。由于本项目目标为「要联网/远程/多设备」，不能像纯本机那样直接砍到 60ms，改为按连接类型分档：本机/局域网 \~80/250，公网 300/900，运行时自适应切换。

**F\. 弱网抗性**

- 联网档开启 Opus inband FEC \+ DTX \+ RTP RED 冗余。LiveKit 支持，本机档可关。

### 2\.5 partial 投机预热详解

> 核心认知：投机预热是**两层**，一层零风险、一层有风险，**别混做**。这是很多实现做崩的根源。
> 
> 

```Plain Text
第 0 层（零风险，无条件尽早做）：纯预热
   VAD 一检测到开口 → warm：TTS 模型/连接预热、LLM 连接建好、system+RAG 先 prefill
   不赌任何内容，无回滚。
第 1 层（有风险，需判稳 + 回滚）：内容投机
   partial 稳定 → 拿「猜的文本」抢跑发 LLM，赌 final ≈ 该 partial，赌错要回滚。
```

**赌注模型 \& 前提量化**

```Plain Text
E = P(命中) × 省下的 LLM 首包延迟
  − P(未命中) × (浪费算力 + 回滚成本 + 可能的用户可见抖动)
```

先量一个数决定值不值得：`T_gap = final 到达时刻 − partial 判稳时刻`。若常态只有 100\-150ms，投机省不了多少、却背回滚复杂度，**不如不做**；有 300ms\+ 才值得赌。

**如何判断 partial 稳定（组合信号，按重要性）**

- **A\. 前缀稳定（最关键）**：流式 ASR 修正的几乎总是尾部，头部很少动。维护最近 K 帧 partial 的**最长公共前缀（LCP）**，稳定前缀可信、尾部忽略。比「等整句不变」早得多。

- **B\. 尾部静默去抖**：稳定前缀连续 `N ms`（经验 300\-500ms）没变 → 收敛。

- **C\. 接近 endpoint（最强信号）**：VAD 检测到停顿 → final 马上来，`T_gap` 极小、命中率极高，**几乎可无脑抢跑**。

- **D\. 长度门槛**：稳定前缀 `≥ minTokens`（如 4\-6 字）再发，太短上下文不足、易猜歪。

判据：`稳定前缀 ≥ minTokens && 已保持 ≥ N ms && (接近 endpoint 可放宽) && (无 in-flight 投机 或 新前缀更长)`。

**抢跑发请求**

- **版本号 \+ 单飞**：每次投机打单调递增 `specVersion`，同一时刻只允许一个在飞；来了更长稳定前缀就 `abort` 旧的、发新的。

- **副作用按激进度分两档**：

- **诚实限制**：绝大多数托管 LLM **不能给正在生成的请求追加输入 token**，所以「增量前缀投机、final 来了续尾巴」不可行——现实就是「拿稳定前缀当完整输入发，错了 abort 重发」。真正白拿的是 system\+RAG 的 **prefix caching**（与投机不冲突）。

**final 到达后的对齐与回滚**（先 `normalize`：小写化、去标点、trim）

1. **一致（命中）**：投机结果即真结果，直接「放闸」，净赚整个 LLM 首包时间。判稳做得好时应是多数。

2. **final 是投机文本的延长（前缀命中）**：用简单规则判 salvage——尾部新增 token ≤ 阈值 且 不含疑问/否定/新实体 → 保留；否则按未命中处理。拿不准就回滚（保守更安全），别用语义嵌入对比（太慢）。

3. **分歧（未命中）**：丢弃投机，用 final 重发。

回滚动作（按档）：

```Plain Text
未命中回滚：
  ① LLM: inFlight.ctrl.abort()          // 掐掉投机流
  ② 用 final 重新 fireReal()             // 真请求
  保守档到此结束（副作用本就闸着，用户无感）
  激进档额外「自己打断自己」：
  ③ TTS: stop() + flush 播放队列
  ④ 用 requestId/seq 丢弃迟到的投机音频包  // 同豆包 SDK 的终态闸锁思路
  ⑤ 已显示的 displayText 替换 → 有可见闪烁（激进档代价）
```

**状态机**

```Plain Text
VAD 开口
          │
          ▼
    ┌───────────┐   （第0层，无条件）
    │ WARMING   │── 预热 TTS/LLM 连接、prefill system+RAG
    └─────┬─────┘
          │ partial 流入，更新稳定前缀
          ▼
    ┌───────────┐  稳定前缀≥minTokens 且 debounce 到
    │ LISTENING │──────────────┐
    └─────┬─────┘              ▼
          │              ┌───────────┐  更长稳定前缀 → abort 旧、发新
          │              │ SPECULATE │◀─────────┐
          │              │ (LLM在飞) │──────────┘
          │              └─────┬─────┘
          │ final 到达          │ final 到达
          ▼                    ▼
    ┌─────────────────────────────────┐
    │ RECONCILE  比对 final vs specText │
    └───┬───────────┬───────────┬──────┘
    命中│      前缀延长│       分歧│
        ▼           ▼           ▼
    放闸提交    规则判salvage   abort+重发final
   （零延迟）   或降级为分歧    （+激进档自打断）
```

**护栏（避免优化变负优化）**

- 单飞 \+ 限次，靠「更长前缀替换」更新，别并发打爆 LLM 和预算。

- 成本意识：投机 = 多花 token，命中率低时是纯亏钱；埋点监控命中率，低于阈值自动关。

- 优先吃 endpoint 信号（性价比最高）；纯 debounce 触发的更该走保守档。

- 自适应阈值：按最近命中率动态调 `N ms` / `minTokens`（ASR 抖调高、稳调低）。

- 保底不劣化：投机只是可选加速层，任何时候 final 都能走正常路径，做成可一键降级开关。

**落地建议**

- **第 0 层立刻做**：VAD 开口就预热 TTS（GPT\-SoVITS 冷启不便宜）\+ prefill system/RAG，零风险先吃掉。

- **第 1 层从保守档起步**：只跑 LLM、闸住 TTS，先测 `T_gap` 和命中率，好看再对 TTS 上激进档。

- 优先绑 endpoint 触发投机；回滚机制直接复用豆包 SDK 那套（版本号 \+ 终态闸锁 \+ 丢弃迟到包）。

### 2\.6 能力 → 开源替代映射（无任何内部依赖）

---

## 3\. 存在的问题与修复方案

按投入产出比（优先级）排序。

### 3\.1 问题清单与优先级

### 3\.2 重点修复：句子级流水线真正重叠

当前 `startTtsConsumer` 的 `pump()`：

```Plain Text
await tts.speakFromQwenReply({ ... });  // 一直 await 到后端 tts.finished 终态
queue.advanceSend();
pump();                                  // 才开始下一句
```

第 N\+1 句的合成不会启动，直到第 N 句彻底播完 → 总时长 = Σ\(每句 合成 \+ 播放\)，**没有重叠**。

**约束**：GPT\-SoVITS 通常单模型实例，无法真并行合成两句；音频必须顺序播。 **正确做法**：解耦「合成」与「播放」——用预取队列让**合成领先播放一句**，而不是串行等终态。多句长回复时收益明显。

### 3\.3 已做对、需保持的部分

- LiveKit Room 缓存复用（`ensureLiveKitRoomConnected`），ICE/DTLS 握手为启动一次性成本，不摊到每次说话。

- 控制流走 `reliable: true` DataChannel（控制消息不能丢），媒体走 RTP/SRTP，两平面分离正确。

- ASR 退回 Electron node 侧（相比塞进 Python 少一跳）。

- HTTP 降级链路保留（`/v3/tts/speak`、`/v3/tts/cancel`），实时失败可回退。

### 3\.4 落地顺序建议

1. **先 P0**：ASR 流式化 \+ endpoint（治语音输入 5\-9s）；barge\-in \+ 耳机门控（治全双工）；同时上**第 0 层纯预热**（零风险白拿）。

2. **再 P1**：TTS 固定 ogg/Opus streaming（治首包）；句子流水线重叠 \+ 短语级喂入（治多句累加）；投机预热**保守档**起步（先测 `T_gap`/命中率）。

3. **后 P2**：抖动缓冲分档；联网档 FEC/RED（治联网弱网）。

4. **架构并行推进**：控制面板改 sharedStore 读写方；orchestration 留 renderer 并预留总线入口（为 Agent 形态铺路）。

5. **北极星（远期）**：观望端到端语音大模型，替代级联。

